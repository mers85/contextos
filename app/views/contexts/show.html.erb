<p>
  <strong>Title:</strong>
  <%= @context.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @context.description %>
</p>
<%= link_to 'Edit', edit_context_path(@context) %>
<%= link_to 'Back', contexts_path %>
